#include <bits/stdc++.h>
using namespace std;

int main(){
    string s, t = "";
    cin >> s;
    int sz = s.size();
    int cnt = 0;
    for(int i = 0; i < sz; i++){

        if(s[i] == 'Q'){
            t += s[i];
        }
        else if(s[i] = 'A'){
            
        }
    }

}